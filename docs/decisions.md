# decisions.md — Память проекта

> Этот файл читается в начале каждой рабочей сессии с AI.
> Каждое принятое решение фиксируется здесь с обоснованием.
> Без этого файла AI не имеет контекста проекта.

---

## Продукт

**Суть:** инструмент автоматической проверки публичных сайтов на соответствие 152-ФЗ (персональные данные, РФ).

**Аналогии:**
- 1С — пользователь не думает об изменениях законов, обновления приходят изнутри
- Гарант — подписочная модель, актуальность базы

**Пользователь вводит URL — получает отчёт:** что нарушено, где, какой штраф, как исправить.

---

## Пользователи

- Разработчик сайта
- Юрист
- Владелец сайта

Все три — не технические специалисты в части 152-ФЗ. Отчёт должен быть понятен без знания закона.

---

## Бизнес-модель

Коммерческий продукт. Три модели монетизации:
1. Подписка на инструмент (десктоп / онлайн)
2. Подписка на обновления базы критериев
3. Разовая оплата за отчёт (без покупки продукта — клиент платит за результат)

---

## Функциональность

- Проверка одного сайта по URL
- Проверка списка сайтов по шаблону
- Разовый отчёт
- История проверок (динамика соответствия)
- Выбор блоков проверки (А-Ж) или полная проверка

---

## База критериев 152-ФЗ

Живёт в открытых источниках:
- https://rpn.gov.ru/upload/iblock/3cc/jy73a14zc99w4bm8xnc22y2m7iwpusar/248_FZ.pdf
- https://minfin.gov.ru/ru/document?id_4=301238
- https://digital.gov.ru/documents/o-personalnyh-dannyh/

Структура критериев — блоки А-Ж (выявлены в предыдущем диалоге).

**Программа сама следит за изменениями законов** и обновляет базу критериев (модуль Law Monitor — не в MVP).

---

## Платформа

- **Сейчас:** веб (локальный или онлайн)
- **Перспектива:** десктопное приложение (обёртка над веб-версией через Electron или Tauri)
- **Название релиза:** на кириллице (конкретное название — TODO)

---

## Стек

| Компонент | Технология | Обоснование |
|-----------|-----------|-------------|
| Сканирование | Python + Playwright | Видит динамический JS-контент (cookie-баннеры, формы). requests+BeautifulSoup не видит JS-рендеринг |
| API | FastAPI | async важен при параллельном сканировании. Автодокументация нужна для модели "оплата за отчёт" |
| БД | SQLite → PostgreSQL | SQLite для старта (нет сервера, просто). Миграция на PostgreSQL при появлении подписочной модели |
| UI | React | Однородный JS на фронте |
| Десктоп (перспектива) | Electron или Tauri | Оборачивает веб-версию, не нужно писать заново |

---

## Команда

Один человек + AI (Claude). Команды нет. Это принципиально.

**Следствие:** стек простой и однородный. Структура репозитория — это память проекта. Каждая сессия начинается с передачи AI файлов из `docs/`.

---

## Архитектура — модули

| Модуль | Назначение | В MVP |
|--------|-----------|-------|
| Scanner | Принимает URL, загружает страницу, прогоняет детекторы | ✅ |
| Detector Engine | Набор детекторов по блокам А-Ж, включается/выключается независимо | ✅ |
| Law Base | База критериев: нормы, штрафы, ссылки на статьи | ✅ |
| Report Engine | Формирует читаемый отчёт из результатов сканера | ✅ |
| UI | Веб-интерфейс: поле URL + кнопка + результат | ✅ |
| Law Monitor | Мониторинг изменений в источниках законов | ❌ |
| History | История проверок, динамика | ❌ |
| Auth + Billing | Подписка, доступ, оплата | ❌ |

**Принцип:** Scanner не знает ничего про UI и API. Модули независимы.

---

## Дорожная карта

**Этап 0 — Инфраструктура**
GitHub repo, структура папок, ветки, защита main, CI (линтер + тесты), шаблоны Issues и PR.

**Этап 1 — Detector Engine + Law Base (параллельно)**
Один детектор = одна запись в Law Base = два теста (нарушение + соответствие).
Фабричный паттерн. Только статически детектируемые критерии из блоков А-Ж.

**Этап 2 — Scanner + Report Engine + минимальный UI (параллельно)**
Рабочий продукт который можно показать пользователю.
Форматы отчёта: веб + JSON.

**Этап 3 — Доработка UI**
На основе обратной связи от первых пользователей. Подготовка к онлайн-деплою.

---

## Структура репозитория

```
project/
│
├── .github/
│   ├── ISSUE_TEMPLATE/
│   ├── PULL_REQUEST_TEMPLATE.md
│   └── workflows/
│       ├── lint.yml
│       └── test.yml
│
├── docs/
│   ├── decisions.md      ← этот файл
│   ├── roadmap.md
│   ├── architecture.md
│   └── todo.md
│
├── law_base/
│   ├── blocks/
│   │   ├── A.yaml
│   │   ├── B.yaml
│   │   └── ...
│   └── sources.yaml
│
├── scanner/
│   ├── detectors/
│   │   ├── base.py
│   │   ├── block_a.py
│   │   └── ...
│   ├── engine.py
│   └── playwright_wrapper.py
│
├── api/
│   ├── main.py
│   ├── routes/
│   └── models/
│
├── report/
│   ├── engine.py
│   └── templates/
│
├── ui/
│   └── (React)
│
├── db/
│   ├── models.py
│   └── migrations/
│
├── tests/
│   ├── detectors/
│   │   ├── fixtures/
│   │   └── test_block_a.py
│   └── api/
│
├── README.md
├── CHANGELOG.md
└── requirements.txt
```

---

## Law Base MVP — верифицированные критерии

Источник: КонсультантПлюс, КоАП РФ ст. 13.11, актуальная редакция с учётом ФЗ-420 от 30.11.2024 (вступил в силу 30.05.2025).
Штрафы указаны для юридических лиц.

| ID | Нарушение | Норма 152-ФЗ | Статья КоАП | Штраф (юрлицо) |
|----|-----------|-------------|-------------|----------------|
| A1 | Нет политики ПДн / нет публичного доступа к ней | ст. 18.1 ч.2 | ч.3 ст.13.11 | 30 000 – 60 000 руб. |
| B1 | Чекбокс согласия предустановлен (checked) | ст. 9 | ч.2 ст.13.11 | 300 000 – 700 000 руб. |
| B2 | Форма с ПДн без чекбокса согласия | ст. 9 | ч.2 ст.13.11 | 300 000 – 700 000 руб. |
| B3 | Нет ссылки на политику рядом с формой (нарушение состава согласия) | ст. 9 ч.2 | ч.2 ст.13.11 | 300 000 – 700 000 руб. |
| C1 | Аналитика / cookie без согласия пользователя | ст. 9 | ч.1 ст.13.11 | 150 000 – 300 000 руб. |
| C2 | Зарубежная аналитика — нарушение локализации ПДн | ст. 18 ч.5 | ч.8 ст.13.11 | 1 000 000 – 6 000 000 руб. |

**Принцип верификации:** каждый критерий привязан к конкретной части статьи КоАП. Сумма штрафа берётся из первоисточника, не из вторичных статей. При любом сомнении — проверять КонсультантПлюс напрямую.

---

## TODO

- [ ] Название продукта (на кириллице)
- [ ] Видимость репозитория (private / public)
- [ ] Law Monitor — мониторинг изменений законов (после MVP)
- [ ] History — история проверок (после MVP)
- [ ] Auth + Billing — подписка и оплата (после MVP)
- [ ] Десктопная упаковка через Electron или Tauri (после MVP)
- [ ] Миграция БД с SQLite на PostgreSQL (при появлении подписочной модели)
- [ ] **D1 (HTTPS)** — требует юридического подтверждения практики. Прямой нормы "сайт обязан использовать HTTPS" в 152-ФЗ нет. Нарушение вытекает косвенно: нет HTTPS → данные в открытом виде → нарушение ст. 19 152-ФЗ (технические меры защиты) → ч.6 ст.13.12 КоАП (50 000 – 100 000 руб. для юрлиц). Включить после подтверждения правоприменительной практики.
- [ ] **E1 (уведомление РКН)** — не детектируется по HTML. Требует обращения к реестру операторов ПДн на сайте РКН (rkn.gov.ru). Необходимо: изучить наличие публичного API или возможности парсинга реестра, оценить надёжность. Включить после решения технической проблемы доступа к реестру.

---

## Правила работы с AI

1. **Новый диалог — первым делом передать `docs/START.md`**. AI читает его и запрашивает нужные файлы.
2. Каждое принятое решение фиксировать в этом файле сразу
3. После каждой сессии обновить раздел "Текущий статус" в `docs/START.md`
4. Каждый модуль разрабатывается отдельно в отдельной сессии
5. AI не поддакивает — критически оценивает предложения
6. Фантазия = ошибка. Только факты из контекста
7. Штрафы и нормы — только из первоисточника, не из вторичных статей
8. **Перед отправкой ответа — проверить полноту:** если задача затрагивает несколько файлов, все они должны быть обновлены. Частичное выполнение = ошибка.
