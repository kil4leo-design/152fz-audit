# START.md — Точка входа для новой сессии

> Один файл. Весь контекст здесь.
> Передай этот файл первым в любом новом диалоге — Claude.ai или Claude Code.

---

## Инструкция для AI — читать до любого действия

Ты работаешь над коммерческим продуктом — инструментом проверки сайтов на соответствие
152-ФЗ (РФ, персональные данные). Один разработчик + ты. Команды нет.

**До начала работы запроси у пользователя:**
```
Нужны файлы:
1. docs/decisions.md
2. docs/roadmap.md
3. docs/architecture.md
```
Без них — фантазия. Фантазия = ошибка. открой директорию docs, читай файлы, подтверждай понимание если понял, иначе переспроси.
Не начинай работу до подтверждения пользователя.

---

## Частные случаи

Неоднозначные срабатывания детекторов, пограничные ситуации, вопросы требующие исследования —
фиксировать в `docs/edge_cases.md` с порядковым номером EC-N и полным контекстом.
Не пытаться решить сразу — зафиксировать для последующего исследования.

---

## Правила (без исключений)

1. **Не поддакивать.** Видишь ошибку в предложении — говори прямо.
2. **Фантазия = ошибка.** Только факты из файлов репо и контекста сессии.
3. **Штрафы и нормы** — только из первоисточника (КонсультантПлюс, КоАП). Не из статей.
4. **Код — только после аудита:** черновик → полный построчный аудит → исправить → отдавать.
5. **Полнота:** задача затрагивает несколько файлов — обновить все. Частичное = ошибка.
6. **Один модуль = одна сессия.**
7. **Коммит:** один логический блок = один коммит. Формат команды всегда прилагается.
8. **Push запрещён без явного запроса.** `git push` — только по команде пользователя. AI предлагает, не выполняет.

---

## Репозиторий

https://github.com/kil4leo-design/152fz-audit  
Рабочая ветка: `dev` → PR → `main`

---

## Текущий статус

**Дата обновления:** 2026-05-24 (сессия 11, финал)

**Завершены:** Этап 0, Этап 1, Этап 2, большая часть Этапа 3.

Детекторы: A1, B1, B2, B3, C1 (26 тестов). C2 отключён (enabled: false).
PlaywrightWrapper: stealth mode, robots.txt Вариант B, WAF detection.
Report: violations + recommendations + passed + waf_blocked + blocked_excerpt.
UI: все статусы отображаются — нарушения (красные), рекомендации (оранжевые),
пройденные проверки (зелёные), WAF-блок (фиолетовый), robots_warning (синий).
UI: подсказка про режим инкогнито под полем ввода URL.
Disclaimer: фраза про инкогнито с объяснением причины.
B2 evidence: matched_fields — список найденных полей ПДн. EC-2 закрыт.

**Исправления сессии 11:**
- C1: различает загрузку скрипта (рекомендация) и реальный сбор данных (нарушение). EC-1 закрыт.
- A1: benefit of doubt при 429/5xx в verify_accessible (шаг 4).
  _is_accessible (шаг 3) строгий — только 200 = нашли.
- PlaywrightWrapper: обработка ошибки «navigating» при page.content() (JS-редиректы).
- B2: matched_fields в evidence. EC-2 закрыт.

**Запуск продукта (два терминала, из корня проекта):**
```bash
# Терминал 1 — API
cd /home/k/152fz-audit/152fz-audit && uvicorn api.main:app --reload

# Терминал 2 — UI
cd /home/k/152fz-audit/152fz-audit/ui && npm run dev
# → http://localhost:5173 (если порт занят — 5174)
```

**Ветка:** dev. Всё запушено. Последний коммит: b7f97d1.

**Открытые вопросы:** нет.

**Следующие задачи (Этап 3):**
- История проверок (модуль History)
- Подготовка к онлайн-деплою
- Документация для пользователя
