# START.md — Точка входа для новой сессии

> Один файл. Весь контекст здесь.
> Передай этот файл первым в любом новом диалоге — Claude.ai или Claude Code.

---

## Инструкция для AI — читать до любого действия

Ты работаешь над коммерческим продуктом — инструментом проверки сайтов на соответствие
152-ФЗ (РФ, персональные данные). Один разработчик + ты. Команды нет.

**До начала работы запроси у пользователя:**
```
Нужны файлы:
1. docs/decisions.md
2. docs/roadmap.md
3. docs/architecture.md
```
Без них — фантазия. Фантазия = ошибка. открой директорию docs, читай файлы, подтверждай понимание если понял, иначе переспроси.
Не начинай работу до подтверждения пользователя.

---

## Правила (без исключений)

1. **Не поддакивать.** Видишь ошибку в предложении — говори прямо.
2. **Фантазия = ошибка.** Только факты из файлов репо и контекста сессии.
3. **Штрафы и нормы** — только из первоисточника (КонсультантПлюс, КоАП). Не из статей.
4. **Код — только после аудита:** черновик → полный построчный аудит → исправить → отдавать.
5. **Полнота:** задача затрагивает несколько файлов — обновить все. Частичное = ошибка.
6. **Один модуль = одна сессия.**
7. **Коммит:** один логический блок = один коммит. Формат команды всегда прилагается.

---

## Репозиторий

https://github.com/kil4leo-design/152fz-audit  
Рабочая ветка: `dev` → PR → `main`

---

## Текущий статус

**Дата обновления:** 2026-05-22 (сессия 6)

**Завершены:** Этап 0, Law Base MVP (A/B/C.yaml), BaseDetector, DetectorEngine, детекторы A1 + B1 + B2 + B3 + C1, тесты 18/18. Аудиты: исправлены B2 severity mutation, морфология YAML, покрытие alternative_consent, evidence["found"] удалён, C2 отключён (false positive риск), C1 domain substring match, B1/B2/B3 pd_fields word-boundary.

**В работе:** Этап 1 — Detector Engine.
**Следующий шаг:** `scanner/playwright_wrapper.py` — единственный оставшийся блокер Этапа 1.
PlaywrightWrapper: robots.txt check → network interception → page.goto(networkidle+2s) → (html, network_log).
Архитектура зафиксирована в architecture.md → раздел "PlaywrightWrapper — обязательное поведение".
